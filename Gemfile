source "https://rubygems.org"

# Matches the GitHub Pages native build environment, so local previews
# behave like the deployed site.
gem "github-pages", group: :jekyll_plugins

# Required to run `jekyll serve` locally on Ruby 3.0+.
gem "webrick"
